<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:loc="clr-namespace:Fridge_Shopping_app"
             x:Class="Fridge_Shopping_app.EditorPage"
             Title="EditorPage"
             x:DataType="loc:EditorPageViewModel">

    <ContentPage.BindingContext>
        <loc:EditorPageViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
        <Style TargetType="Label" x:Key="mainLabel">
            <Setter Property="BackgroundColor" Value="AliceBlue" />
            <Setter Property="FontSize" Value="Large" />
            <Setter Property="FontAttributes" Value="Bold" />
        </Style>
    </ContentPage.Resources>

    <VerticalStackLayout>
        <Label Text="Name" Style="{StaticResource mainLabel}" />
        <Entry Text="{Binding Item.Name}" />
        <Label Text="Quantity" Style="{StaticResource mainLabel}" />
        <Entry Text="{Binding Item.Quantity}" />
        <Label Text="Expiration date" Style="{StaticResource mainLabel}" />
        <DatePicker Date="{Binding Item.Exp_date}" />
        <Button Text="Save" Command="{Binding SaveCommand}" />
    </VerticalStackLayout>

</ContentPage>